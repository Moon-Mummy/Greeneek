pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx}"
      run: pnpm lint
    typecheck:
      glob: "*.{ts,tsx}"
      run: pnpm typecheck
    brand-sweep:
      run: ./scripts/brand-sweep.sh
