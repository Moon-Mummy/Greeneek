# Keyless filesystem snapshots combine spill settings with replay. The
# sandboxed filesystem stack already lives in `gnk-base`; the ACP row re-pins
# `greeneek-v4-flash` for the recorded corpus.
- id: llm-greeneek
  name: '@greeneek/gnk-llm-greeneek'
  disabled: true

- id: agent-default-model
  name: '@greeneek/gnk-agent-default-model'
  config:
    provider: greeneek-official
    model: greeneek-v4-flash

- id: session-persistence-jsonl
  name: '@greeneek/gnk-session-persistence-jsonl'
  config:
    root: !!js gnkHomePath('sessions')
    compression: none

- id: agent-instructions
  name: '@greeneek/gnk-agent-instructions'
  config:
    maxBytes: 65536

- id: system-prompt
  name: '@greeneek/gnk-system-prompt'
  config:
    persona: |
      You are a coding assistant powered by the {{model}} model. Your working directory is {{cwd}}. Your bash tool runs under a file sandbox — a `[sandbox: file access denied …]` result is policy, not a command bug.

      Verify your work by running the code or tests. Keep answers brief and factual.

- insert:
    - id: llm-replay
      name: '@greeneek/gnk-llm-replay'
      config:
        providers:
          - id: greeneek-official
            name: Greeneek
            models:
              - id: greeneek-v4-flash
              - id: greeneek-v4-pro

- id: spill-local
  name: '@greeneek/gnk-spill-local'
  config:
    root: !!js process.env.GNK_SNAPSHOT_SPILL_ROOT ?? './.spill'

- id: spill-policy
  name: '@greeneek/gnk-spill-policy'
  config:
    maxInlineBytes: 800
