# Replay-backed goal patch over the shipped headless profile.
- id: headless-startup
  disabled: true

- id: headless-runner
  disabled: true

- id: llm-greeneek
  disabled: true

- id: agent-loop
  config:
    agents:
      - id: main
        provider: greeneek-official
        model: greeneek-v4-flash
        cwd: !!js process.cwd()

- id: session-persistence-jsonl
  config:
    root: './.sessions'
    compression: none

- insert:
    - id: llm-replay
      name: '@greeneek/gnk-llm-replay'
