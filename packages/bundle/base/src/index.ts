/**
 * @greeneek/gnk-base — the shared gnk core as a profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the `gnk.bundle.patch`
 * manifest field and resolved by the profile composer through that field;
 * this module carries no runtime API.
 * @module @greeneek/gnk-base
 */

export {}
